n = 100;
t = 0:Ts:(n);
r = zeros(n, 5);
r(:, 2) = 2;
d = zeros(n, 3);

[x, u, y, z, zPredTraj, uPredTraj] = MPCSim(md_quadcopter, r, d);
tz = t(1:size(z,1));

figure();
plot(tz, z(:,3);
hold on;
plot(simmeasured.time, simmeasured.data);

figure();
tu = t(1:size(u,1));
plot(tu, u);
